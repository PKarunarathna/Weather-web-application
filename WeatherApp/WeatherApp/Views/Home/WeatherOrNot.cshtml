
@{
    ViewBag.Title = "Weather Or Not";
}

<h2>WeatherOrNot</h2>
<strong> City:</strong><input id="txtCity" type="text" />
<br />
<br />
City:<p id="name"></p>
<button>Search</button>
<div class="col-md-12">
    <h5> Current conditions in <span data-bind="text:name"></span></h5>
</div>
<div class="col-md-12">
    Temperature is <span data-bind="text:main.temp"></span>&deg; F
</div>
<div class="col-md-12">
    Humidity is <span data-bind="text:main.humidity"></span>
</div>
<div class="col-md-12">
   Speed of wind is <span data-bind="text:wind.speed"></span>
</div>


<script>
    var weather = Object();
    $(document).ready(function () {
        
            $.get("@Url.Action("GetWeather","Home")", function (respone) {
                console.log(response);
                weather = ko.mapping.fromJS(response);
                ko.applyBindings(weather);
            });
            });
      
</script>